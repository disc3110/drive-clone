<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen bg-slate-950 text-slate-100">
    <main class="max-w-3xl mx-auto px-4 py-10">
      <a
        href="<%= file.folder ? '/folders/' + file.folderId : '/' %>"
        class="text-xs text-slate-400 hover:text-slate-200"
      >
        ← Back
      </a>

      <div class="mt-3 rounded-xl bg-slate-900/70 border border-slate-800 p-6">
        <h1 class="text-2xl font-semibold mb-2">
          <%= file.name || file.originalName %>
        </h1>

        <% if (file.folder) { %>
          <p class="text-xs text-slate-400 mb-1">
            In folder:
            <a
              href="/folders/<%= file.folder.id %>"
              class="underline hover:text-slate-100"
            >
              <%= file.folder.name %>
            </a>
          </p>
        <% } %>

        <dl class="mt-4 space-y-1 text-sm text-slate-300">
          <div class="flex justify-between">
            <dt class="text-slate-400">Original name</dt>
            <dd><%= file.originalName %></dd>
          </div>
          <div class="flex justify-between">
            <dt class="text-slate-400">Size</dt>
            <dd><%= (file.size / 1024).toFixed(1) %> KB</dd>
          </div>
          <div class="flex justify-between">
            <dt class="text-slate-400">MIME type</dt>
            <dd><%= file.mimeType %></dd>
          </div>
          <div class="flex justify-between">
            <dt class="text-slate-400">Uploaded at</dt>
            <dd><%= file.createdAt.toISOString().slice(0, 19).replace('T', ' ') %></dd>
          </div>
        </dl>

        <% if (typeof errors !== 'undefined' && errors && errors.length) { %>
          <div class="mt-4 space-y-1">
            <% errors.forEach(err => { %>
              <p class="text-xs text-red-300">• <%= err.msg %></p>
            <% }) %>
          </div>
        <% } %>

        <div class="mt-6">
          <a
            href="/files/<%= file.id %>/download"
            class="inline-flex items-center rounded-md bg-indigo-500 hover:bg-indigo-400 px-4 py-2 text-sm font-medium"
          >
            Download
          </a>
        </div>
      </div>
    </main>
  </body>
</html>