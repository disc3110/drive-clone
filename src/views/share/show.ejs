<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen bg-slate-950 text-slate-100">
    <main class="max-w-4xl mx-auto px-4 py-10">
      <h1 class="text-2xl font-semibold mb-2">
        Shared folder: <%= folder.name %>
      </h1>

      <p class="text-xs text-slate-400 mb-6">
        Expires at: <%= expiresAt.toISOString().slice(0, 19).replace('T', ' ') %>
      </p>

      <% if (folder.files.length) { %>
        <ul class="space-y-2">
          <% folder.files.forEach(file => { %>
            <li class="flex justify-between items-center rounded-md bg-slate-900/60 border border-slate-800 px-4 py-2 text-sm">
              <span>
                <%= file.name || file.originalName %>
                <span class="text-xs text-slate-400">
                  (<%= (file.size / 1024).toFixed(1) %> KB)
                </span>
              </span>

              <a
                href="/files/<%= file.id %>/download"
                class="text-indigo-400 hover:underline"
              >
                Download
              </a>
            </li>
          <% }) %>
        </ul>
      <% } else { %>
        <p class="text-sm text-slate-400">This folder is empty.</p>
      <% } %>
    </main>
  </body>
</html>