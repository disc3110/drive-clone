<nav class="bg-slate-900 border-b border-slate-800">
  <div class="max-w-5xl mx-auto flex items-center justify-between px-4 py-3">
    <a href="/" class="font-semibold tracking-tight">
      Drive Clone
    </a>

    <div class="flex items-center gap-4 text-sm">
      <% if (user) { %>
        <a href="/" class="hover:text-slate-200 text-slate-300">
          Home
        </a>
        <a href="/folders" class="hover:text-slate-200 text-slate-300">
          Folders
        </a>
        <a href="/files/upload" class="hover:text-slate-200 text-slate-300">
          Upload
        </a>

        <form action="/logout" method="POST">
          <button
            class="px-3 py-1.5 rounded-md bg-slate-700 hover:bg-slate-600"
            type="submit"
          >
            Logout
          </button>
        </form>
      <% } else { %>
        <a href="/login" class="hover:text-slate-200 text-slate-300">
          Login
        </a>
        <a
          href="/register"
          class="px-3 py-1.5 rounded-md bg-indigo-500 hover:bg-indigo-400"
        >
          Sign up
        </a>
      <% } %>
    </div>
  </div>
</nav>