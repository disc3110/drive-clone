<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="min-h-screen flex items-center justify-center bg-slate-950 text-slate-100">
    <div class="w-full max-w-md bg-slate-900/70 border border-slate-800 rounded-xl p-6 shadow">
      <h1 class="text-2xl font-semibold mb-4 text-center">Create an account</h1>

          <% if (errors && errors.length) { %>
            <div class="bg-red-100 text-red-800 text-sm px-3 py-2 rounded">
              <ul class="list-disc list-inside">
                <% errors.forEach(err => { %>
                  <li><%= err.msg %></li>
                <% }) %>
              </ul>
            </div>
          <% } %>

      <form action="/register" method="POST" class="space-y-4">
        <div>
          <label class="block text-sm mb-1" for="name">Name (optional)</label>
          <input
            class="w-full rounded-md bg-slate-900 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
            type="text"
            id="name"
            name="name"
          />
        </div>

        <div>
          <label class="block text-sm mb-1" for="email">Email</label>
          <input
            class="w-full rounded-md bg-slate-900 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
            type="email"
            id="email"
            name="email"
            required
          />
        </div>

        <div>
          <label class="block text-sm mb-1" for="password">Password</label>
          <input
            class="w-full rounded-md bg-slate-900 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
            type="password"
            id="password"
            name="password"
            required
          />
        </div>

        <div>
          <label class="block text-sm mb-1" for="confirmPassword">Confirm password</label>
          <input
            class="w-full rounded-md bg-slate-900 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            required
          />
        </div>

        <button
          class="w-full mt-2 rounded-md bg-indigo-500 hover:bg-indigo-400 px-3 py-2 text-sm font-medium"
          type="submit"
        >
          Sign up
        </button>
      </form>

      <p class="mt-4 text-center text-xs text-slate-400">
        Already have an account?
        <a href="/login" class="text-indigo-400 hover:underline">Log in</a>
      </p>
    </div>
  </body>
</html>